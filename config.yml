logging:
  level: INFO
  loggers:
    com.tcc: DEBUG

database:
  driverClass: org.h2.Driver
  user: sa
  password:
  url: jdbc:h2:~/catalogo-data-base
  validationQuery: "SELECT 1"
  properties:
    charSet: UTF-8
    hibernate.dialect: org.hibernate.dialect.H2Dialect

